version: "3.9"

services:
  webapp:
    container_name: one-review-api
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5001:5001"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - Database__ConnectionStrings__DefaultConnection=Host=db;Port=5432;Database=one_review_db;Username=postgres;Password=postgres
    depends_on:
      db:
        condition: service_healthy

  db:
    container_name: one-review-db
    image: postgres:latest
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=one_review_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg-isready -U postgres -d one_review_db"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
